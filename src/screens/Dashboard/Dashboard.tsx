import { ILoginProps } from 'interfaces/screens/Login.interface';
import React from 'react';

// eslint-disable-next-line
function Dashboard(props: ILoginProps) {
  return <div>Dashboard</div>;
}

export default Dashboard;
